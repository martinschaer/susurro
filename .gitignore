# Build output
build/
Susurro.app/

# Cloned and built by setup.sh — 60 MB and carries its own .git
vendor/

# Interrupted downloads from setup.sh
*.part

# Written by swiftly; the Makefile pins the toolchain path anyway
.swift-version

# Per-machine Claude Code settings
.claude/settings.local.json

.DS_Store

# Note: models and transcripts live in ~/.susurro, outside the repo.
